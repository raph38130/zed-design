// ==============================================================
// File generated by Vivado(TM) HLS - High-Level Synthesis from C, C++ and SystemC
// Version: 2016.3
// Copyright (C) 1986-2016 Xilinx, Inc. All Rights Reserved.
// ==============================================================

#include <systemc>
#include <iostream>
#include <cstdlib>
#include <cstddef>
#include <stdint.h>
#include "SysCFileHandler.h"
#include "ap_int.h"
#include "ap_fixed.h"
#include <complex>
#include <stdbool.h>
#include "autopilot_cbe.h"
#include "ap_stream.h"
#include "hls_stream.h"
#include "hls_half.h"
#include "hls_signal_handler.h"

using namespace std;
using namespace sc_core;
using namespace sc_dt;


// [dump_struct_tree [build_nameSpaceTree] dumpedStructList] ---------->


// [dump_enumeration [get_enumeration_list]] ---------->


// wrapc file define: "a"
#define AUTOTB_TVIN_a  "../tv/cdatafile/c.fill.autotvin_a.dat"
// wrapc file define: "buf_r"
#define AUTOTB_TVOUT_buf_r  "../tv/cdatafile/c.fill.autotvout_buf_r.dat"
#define AUTOTB_TVIN_buf_r  "../tv/cdatafile/c.fill.autotvin_buf_r.dat"

#define INTER_TCL  "../tv/cdatafile/ref.tcl"

// tvout file define: "buf_r"
#define AUTOTB_TVOUT_PC_buf_r  "../tv/rtldatafile/rtl.fill.autotvout_buf_r.dat"

class INTER_TCL_FILE {
	public:
		INTER_TCL_FILE(const char* name) {
			mName = name;
			a_depth = 0;
			buf_r_depth = 0;
			trans_num =0;
		}

		~INTER_TCL_FILE() {
			mFile.open(mName);
			if (!mFile.good()) {
				cout << "Failed to open file ref.tcl" << endl;
				exit (1);
			}
			string total_list = get_depth_list();
			mFile << "set depth_list {\n";
			mFile << total_list;
			mFile << "}\n";
			mFile << "set trans_num "<<trans_num<<endl;
			mFile.close();
		}

		string get_depth_list () {
			stringstream total_list;
			total_list << "{a " << a_depth << "}\n";
			total_list << "{buf_r " << buf_r_depth << "}\n";
			return total_list.str();
		}

		void set_num (int num , int* class_num) {
			(*class_num) = (*class_num) > num ? (*class_num) : num;
		}
	public:
		int a_depth;
		int buf_r_depth;
		int trans_num;

	private:
		ofstream mFile;
		const char* mName;
};

extern void fill (
int a,
int buf[38400]);

void AESL_WRAP_fill (
int a,
int buf[38400])
{
	refine_signal_handler();
	fstream wrapc_switch_file_token;
	wrapc_switch_file_token.open(".hls_cosim_wrapc_switch.log");
	int AESL_i;
	if (wrapc_switch_file_token.good())
	{
		CodeState = ENTER_WRAPC_PC;
		static unsigned AESL_transaction_pc = 0;
		string AESL_token;
		string AESL_num;
		static AESL_FILE_HANDLER aesl_fh;


		// output port post check: "buf_r"
		aesl_fh.read(AUTOTB_TVOUT_PC_buf_r, AESL_token); // [[transaction]]
		if (AESL_token != "[[transaction]]")
		{
			exit(1);
		}
		aesl_fh.read(AUTOTB_TVOUT_PC_buf_r, AESL_num); // transaction number

		if (atoi(AESL_num.c_str()) == AESL_transaction_pc)
		{
			aesl_fh.read(AUTOTB_TVOUT_PC_buf_r, AESL_token); // data

			sc_bv<32> *buf_r_pc_buffer = new sc_bv<32>[38400];
			int i = 0;

			while (AESL_token != "[[/transaction]]")
			{
				bool no_x = false;
				bool err = false;

				// search and replace 'X' with "0" from the 1st char of token
				while (!no_x)
				{
					size_t x_found = AESL_token.find('X');
					if (x_found != string::npos)
					{
						if (!err)
						{
							cerr << "WARNING: [SIM 212-201] RTL produces unknown value 'X' on port 'buf_r', possible cause: There are uninitialized variables in the C design." << endl;
							err = true;
						}
						AESL_token.replace(x_found, 1, "0");
					}
					else
					{
						no_x = true;
					}
				}

				no_x = false;

				// search and replace 'x' with "0" from the 3rd char of token
				while (!no_x)
				{
					size_t x_found = AESL_token.find('x', 2);

					if (x_found != string::npos)
					{
						if (!err)
						{
							cerr << "WARNING: [SIM 212-201] RTL produces unknown value 'X' on port 'buf_r', possible cause: There are uninitialized variables in the C design." << endl;
							err = true;
						}
						AESL_token.replace(x_found, 1, "0");
					}
					else
					{
						no_x = true;
					}
				}

				// push token into output port buffer
				if (AESL_token != "")
				{
					buf_r_pc_buffer[i] = AESL_token.c_str();
					i++;
				}

				aesl_fh.read(AUTOTB_TVOUT_PC_buf_r, AESL_token); // data or [[/transaction]]

				if (AESL_token == "[[[/runtime]]]" || aesl_fh.eof(AUTOTB_TVOUT_PC_buf_r))
				{
					exit(1);
				}
			}

			// ***********************************
			if (i > 0)
			{
				// RTL Name: buf_r
				{
					// bitslice(31, 0)
					// {
						// celement: buf(31, 0)
						// {
							sc_lv<32>* buf_lv0_0_38399_1 = new sc_lv<32>[38400];
						// }
					// }

					// bitslice(31, 0)
					{
						int hls_map_index = 0;
						// celement: buf(31, 0)
						{
							// carray: (0) => (38399) @ (1)
							for (int i_0 = 0; i_0 <= 38399; i_0 += 1)
							{
								if (&(buf[0]) != NULL) // check the null address if the c port is array or others
								{
									buf_lv0_0_38399_1[hls_map_index++].range(31, 0) = sc_bv<32>(buf_r_pc_buffer[hls_map_index].range(31, 0));
								}
							}
						}
					}

					// bitslice(31, 0)
					{
						int hls_map_index = 0;
						// celement: buf(31, 0)
						{
							// carray: (0) => (38399) @ (1)
							for (int i_0 = 0; i_0 <= 38399; i_0 += 1)
							{
								// sub                    : i_0
								// ori_name               : buf[i_0]
								// sub_1st_elem           : 0
								// ori_name_1st_elem      : buf[0]
								// output_left_conversion : buf[i_0]
								// output_type_conversion : (buf_lv0_0_38399_1[hls_map_index++]).to_uint64()
								if (&(buf[0]) != NULL) // check the null address if the c port is array or others
								{
									buf[i_0] = (buf_lv0_0_38399_1[hls_map_index++]).to_uint64();
								}
							}
						}
					}
				}
			}

			// release memory allocation
			delete [] buf_r_pc_buffer;
		}

		AESL_transaction_pc++;
	}
	else
	{
		CodeState = ENTER_WRAPC;
		static unsigned AESL_transaction;

		static AESL_FILE_HANDLER aesl_fh;

		// "a"
		char* tvin_a = new char[50];
		aesl_fh.touch(AUTOTB_TVIN_a);

		// "buf_r"
		char* tvin_buf_r = new char[50];
		aesl_fh.touch(AUTOTB_TVIN_buf_r);
		char* tvout_buf_r = new char[50];
		aesl_fh.touch(AUTOTB_TVOUT_buf_r);

		CodeState = DUMP_INPUTS;
		static INTER_TCL_FILE tcl_file(INTER_TCL);
		int leading_zero;

		// [[transaction]]
		sprintf(tvin_a, "[[transaction]] %d\n", AESL_transaction);
		aesl_fh.write(AUTOTB_TVIN_a, tvin_a);

		sc_bv<32> a_tvin_wrapc_buffer;

		// RTL Name: a
		{
			// bitslice(31, 0)
			{
				// celement: a(31, 0)
				{
					// carray: (0) => (0) @ (0)
					{
						// sub                   : 
						// ori_name              : a
						// sub_1st_elem          : 
						// ori_name_1st_elem     : a
						// regulate_c_name       : a
						// input_type_conversion : a
						if (&(a) != NULL) // check the null address if the c port is array or others
						{
							sc_lv<32> a_tmp_mem;
							a_tmp_mem = a;
							a_tvin_wrapc_buffer.range(31, 0) = a_tmp_mem.range(31, 0);
						}
					}
				}
			}
		}

		// dump tv to file
		for (int i = 0; i < 1; i++)
		{
			sprintf(tvin_a, "%s\n", (a_tvin_wrapc_buffer).to_string(SC_HEX).c_str());
			aesl_fh.write(AUTOTB_TVIN_a, tvin_a);
		}

		tcl_file.set_num(1, &tcl_file.a_depth);
		sprintf(tvin_a, "[[/transaction]] \n");
		aesl_fh.write(AUTOTB_TVIN_a, tvin_a);

		// [[transaction]]
		sprintf(tvin_buf_r, "[[transaction]] %d\n", AESL_transaction);
		aesl_fh.write(AUTOTB_TVIN_buf_r, tvin_buf_r);

		sc_bv<32>* buf_r_tvin_wrapc_buffer = new sc_bv<32>[38400];

		// RTL Name: buf_r
		{
			// bitslice(31, 0)
			{
				int hls_map_index = 0;
				// celement: buf(31, 0)
				{
					// carray: (0) => (38399) @ (1)
					for (int i_0 = 0; i_0 <= 38399; i_0 += 1)
					{
						// sub                   : i_0
						// ori_name              : buf[i_0]
						// sub_1st_elem          : 0
						// ori_name_1st_elem     : buf[0]
						// regulate_c_name       : buf
						// input_type_conversion : buf[i_0]
						if (&(buf[0]) != NULL) // check the null address if the c port is array or others
						{
							sc_lv<32> buf_tmp_mem;
							buf_tmp_mem = buf[i_0];
							buf_r_tvin_wrapc_buffer[hls_map_index++].range(31, 0) = buf_tmp_mem.range(31, 0);
						}
					}
				}
			}
		}

		// dump tv to file
		for (int i = 0; i < 38400; i++)
		{
			sprintf(tvin_buf_r, "%s\n", (buf_r_tvin_wrapc_buffer[i]).to_string(SC_HEX).c_str());
			aesl_fh.write(AUTOTB_TVIN_buf_r, tvin_buf_r);
		}

		tcl_file.set_num(38400, &tcl_file.buf_r_depth);
		sprintf(tvin_buf_r, "[[/transaction]] \n");
		aesl_fh.write(AUTOTB_TVIN_buf_r, tvin_buf_r);

		// release memory allocation
		delete [] buf_r_tvin_wrapc_buffer;

// [call_c_dut] ---------->

		CodeState = CALL_C_DUT;
		fill(a, buf);

		CodeState = DUMP_OUTPUTS;

		// [[transaction]]
		sprintf(tvout_buf_r, "[[transaction]] %d\n", AESL_transaction);
		aesl_fh.write(AUTOTB_TVOUT_buf_r, tvout_buf_r);

		sc_bv<32>* buf_r_tvout_wrapc_buffer = new sc_bv<32>[38400];

		// RTL Name: buf_r
		{
			// bitslice(31, 0)
			{
				int hls_map_index = 0;
				// celement: buf(31, 0)
				{
					// carray: (0) => (38399) @ (1)
					for (int i_0 = 0; i_0 <= 38399; i_0 += 1)
					{
						// sub                   : i_0
						// ori_name              : buf[i_0]
						// sub_1st_elem          : 0
						// ori_name_1st_elem     : buf[0]
						// regulate_c_name       : buf
						// input_type_conversion : buf[i_0]
						if (&(buf[0]) != NULL) // check the null address if the c port is array or others
						{
							sc_lv<32> buf_tmp_mem;
							buf_tmp_mem = buf[i_0];
							buf_r_tvout_wrapc_buffer[hls_map_index++].range(31, 0) = buf_tmp_mem.range(31, 0);
						}
					}
				}
			}
		}

		// dump tv to file
		for (int i = 0; i < 38400; i++)
		{
			sprintf(tvout_buf_r, "%s\n", (buf_r_tvout_wrapc_buffer[i]).to_string(SC_HEX).c_str());
			aesl_fh.write(AUTOTB_TVOUT_buf_r, tvout_buf_r);
		}

		tcl_file.set_num(38400, &tcl_file.buf_r_depth);
		sprintf(tvout_buf_r, "[[/transaction]] \n");
		aesl_fh.write(AUTOTB_TVOUT_buf_r, tvout_buf_r);

		// release memory allocation
		delete [] buf_r_tvout_wrapc_buffer;

		CodeState = DELETE_CHAR_BUFFERS;
		// release memory allocation: "a"
		delete [] tvin_a;
		// release memory allocation: "buf_r"
		delete [] tvout_buf_r;
		delete [] tvin_buf_r;

		AESL_transaction++;

		tcl_file.set_num(AESL_transaction , &tcl_file.trans_num);
	}
}

